import React from "react";
import DoLocalStorage from "./doLocalStorage";

const SubmitButton = (props) => {
    // console.log(props.value);
    return <div></div>;
};

export default DoLocalStorage(SubmitButton);
